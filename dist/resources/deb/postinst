#!/bin/sh
set -e

existing=$(which heroku)

if [ "$existing" != "/usr/bin/heroku" ] && [ "$existing" != "" ]; then
    echo "Warning: existing heroku gem detected."
fi

ln -sf /usr/local/heroku/bin/heroku /usr/bin/heroku
